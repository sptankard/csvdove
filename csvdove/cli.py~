
#invoke (from CSVDove):
#cli = CLI(schema, source_files)
#cli.main()
#also, add args for GenStarterSchema

class CLI(object):
    def __init__(self):
        self.target_file_path = 'target.csv'
        self.
    
    def main(self):
        data = data.DataWrapper(bla, bla)
        w = worker.Worker(data_obj)
        w.main()
